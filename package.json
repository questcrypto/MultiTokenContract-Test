{
  "name": "contracts",
  "version": "1.0.0",
  "description": "Acheived: - Role-Based  - contract ERC1155 address is the token holder - tracking supply - Hold Tokens - Pausable functionality in case of bugs or emergencies - Burnable functionality  - Price feature is added for all tokens except for title, and will be emitted each time mint or mintbatch function is called - restricting mint function to one for each right in the property - figure out how to transfer tokens - what will be done with the tokens after minting before deploying the contract top avoid tokens being stuck in contract forever.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "hardhat": "^2.7.0"
  },
  "devDependencies": {
    "@nomiclabs/hardhat-ethers": "^2.0.3",
    "@nomiclabs/hardhat-waffle": "^2.0.1",
    "chai": "^4.3.4",
    "ethereum-waffle": "^3.4.0",
    "ethers": "^5.5.2"
  }
}
